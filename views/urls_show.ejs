<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>TinyApp - Show</title>
  </head>

  <body style="font-size:20px; padding:50px;">
    
    <header>
      <% include ./partials/_header %>
    </header>

    <h3>Tiny URL for:</h3>
    <h3 style="color:blueviolet;"><%= longURL %></h3>
    <h4>Shareable link:</h4>
    <H5 style="color:red;">localhost:8080/u/<%= shortURL %></H5>
    <p>This link has been visited <%= count %> times</p>
    <br>
    <h4>Edit link:</h4>
    <form method="POST" action="/urls/<%= shortURL %>" class="form-inline">
      <div class="form-group mb-2"></div>
        <input class="form-control" type="text" name="longURL" placeholder="http://" style="width: 400px; margin: 1em">
        <div class="input-group-append">
          <button type="submit" class="btn btn-warning" id="button-addon2">Update</button>
        </div>
      </div>
    </form>
      
  </body>
</html>
